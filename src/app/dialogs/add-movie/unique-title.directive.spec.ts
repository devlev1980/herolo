import { UniqueTitleDirective } from './unique-title.directive';

describe('UniqueTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueTitleDirective();
    expect(directive).toBeTruthy();
  });
});
